-- Placeholder — applied directly on remote (out-of-band) on 2026-04-20.
-- Original content created an earlier 1-arg get_calendar_week() RPC.
-- Superseded by 20260420214343_fix_get_calendar_week_user_id_type which
-- replaced it with the current 3-arg text-based signature.
--
-- This file exists so `supabase db push` sees a local-matches-remote history.
-- Full original SQL is recorded in supabase_migrations.schema_migrations on
-- the remote DB; pull via `supabase db query --linked` if needed for local
-- reconstruction.

SELECT 1 WHERE false;
