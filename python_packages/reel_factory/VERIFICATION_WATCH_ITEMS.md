# Verification Watch Items

- Closed in `codex/audit-followups`: ranked-audio sidecar keeps the ranked `track_id` even when the ranked file is missing and mux falls back.
- Closed in `codex/audit-followups`: SQLite raw-connect grep guard now covers all `python_packages` production code with explicit DB-boundary allowlist entries.
- Closed in `codex/audit-followups`: `generate_assets.py --download` help documents that generated-video QC only runs on local downloaded video.
- Deferred until outcomes flow: batch idea diversity is recipe-only; revisit after real recommendation/outcome rows exist.
