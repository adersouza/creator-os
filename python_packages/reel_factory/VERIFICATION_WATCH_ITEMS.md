# Verification Watch Items

- Ranked-audio sidecar should record the ranked `track_id` even when the file is missing and mux fell back.
- Extend the SQLite grep-guard beyond Reel Factory.
- Video-output QC only fires when `--download` runs in the same invocation; document that coupling or decouple it.
