# core.py Decomposition Plan — the Campaign Factory god-class

**Audience:** Codex + owner. **Repo:** creator-os. **Target:** `python_packages/campaign_factory/campaign_factory/core.py` — single class `CampaignFactory`, **lines 623–26,760, ~831 methods**. This is the single biggest lever on the system score (caps Campaign Factory ~7.3 → 9; maint sub-score 4). It is also the **highest-risk** change in the codebase — this plan exists so it's executed with a net, not freehand.

---

## The crux finding (why this is tractable)

A read-only structural audit found the class is **near-stateless orchestration**. True instance state is **only two attributes**:

| Attribute | Refs | Role |
|-----------|------|------|
| `self.conn` | 377 | SQLite connection — the hub every cluster shares |
| `self.settings` | 23 | Config (paths/env) — used by a few clusters |

Everything else that *looks* like shared state (`self._creator_label`, `self._ratio`, …) is a **method call**, not an attribute. **There is no tangled mutable object graph.** Decomposition is therefore **grouping methods that share a DB connection** — not unwinding state. That's a fundamentally safer refactor than a typical god-class.

**Consequence — the target shape:** extract each cluster into its own module that takes `conn` (+ `settings` where needed). `CampaignFactory` becomes a **thin facade** that constructs the modules and **delegates** — its public method signatures stay identical, so **no caller changes**. The god-class shrinks to a composition root.

---

## Cluster map (≈26 modules)

### Tier 1 — lowest risk, extract FIRST (~35 methods, zero cross-cluster calls)
| Cluster | Methods | Lines | New module |
|---------|---------|-------|-----------|
| Graph nodes/edges | 7 | 633–816 | `graph.py` |
| Event pipeline / jobs | 9 | 828–1338 | `events.py` |
| Model/Campaign/Account upserts | 7 | 4206–4377 | `models.py` |
| Asset import | 2 | 4833–4997 | `asset_import.py` |

These depend only on `conn` + `settings`. No circular deps. They prove the pattern.

### Tier 2 — medium risk (~70 methods, manageable cycles via DI)
| Cluster | Methods | Lines | Module | Note |
|---------|---------|-------|--------|------|
| Creative planning | 13 | 890–1241 | `creative_planning.py` | depends on `events` |
| Reference management | 18 | 1142–25357 | `reference.py` | ↔ Audio cycle → inject scorer as callable |
| Caption family | 10 | 5090–25186 | `caption.py` | ↔ Performance cycle → inject scorer as callable |
| Distribution planning | 12 | 4402–21588 | `distribution.py` | called by Inventory + Asset import |
| Decision ledger | 15 | 13257–13619 | `decision_ledger.py` | reporting; isolated helpers |
| Exception management | 11 | 10392–13225 | `exceptions.py` | ↔ Recommendation cycle |
| Discoverability gates | 12 | 10163–12922 | `discoverability.py` | intake/gen/pre-render gates |

### Tier 3 — high risk, extract LAST (~450 methods, orchestration hubs + heavy helpers)
Account-autonomy/trust (15) · Inventory audit (53) · Factory metrics + TribeV2 A/B (45+20) · Audio management (52) · Story management (31) · Variant generation (24) · Account health (7) · Operator review (19) · **Performance analysis (24 — highest fan-in, 5+ caller clusters)** · Lifecycle tracking (23) · Publishing audit (9) · Creative-knowledge analytics (27) · Surface registration (27) · Winner expansion (13) · Carousel integrity (12) · Recommendation lifecycle (12).

> **Hidden region:** lines 10,100–23,400 (~424 methods, ~47% of the body) were scattered/unmapped — they contain 6 first-class domains (Creative-knowledge analytics, TribeV2 A/B, Surface registration, Decision ledger, Winner expansion, Carousel integrity). Surface these as their own modules; don't let them dissolve into Inventory/Factory by accident.

### Shared utilities → a service, not duplication
These are called everywhere (not state): `record_event` (40), `campaign_by_slug` (35), `graph_id_for` (35), `rendered_asset` (24), `ensure_graph_edge` (28). Extract into a `CoreServices`/`Repo` object holding `conn`, injected into every module. Don't copy them.

### Circular dependencies to break (confirmed)
| Cycle | Break with |
|-------|-----------|
| Performance ↔ Caption | inject a `PerformanceScorer` callable into `caption.py` |
| Recommendation ↔ Account | event/callback, not direct call |
| Audio ↔ Reference | inject `AudioScorer` callable into `reference.py` |
| Lifecycle ↔ Performance | Lifecycle reads Performance tables (unidirectional) — keep as read, no back-call |

---

## The non-negotiable safety net — characterization tests FIRST

**No extraction PR may land before its cluster is pinned by characterization tests.** core.py has no real test coverage today; a refactor without a behavior lock is how you silently corrupt the control brain.

- **PR 0 (before any extraction):** build a characterization harness — a seeded SQLite fixture + golden-output tests that call the *current* public methods and snapshot their returns/DB side-effects. Cover at minimum the Tier-1 + Tier-2 clusters' public entry points. These tests must pass **identically** before and after each extraction — that's the definition of done for every move below.
- Run the existing `pipeline_smoke` / `audio_smoke` / contract tests as additional guards.

### Implementation ledger

| Slice | Status | Verification notes |
|-------|--------|--------------------|
| PR 0 — characterization harness | Merged in PR #88 (`98a0530`) | Adds `python_packages/campaign_factory/tests/test_core_characterization.py`, pinning Tier 1 graph/events/jobs/models/asset-import behavior and representative Tier 2 planning/distribution/exception/discoverability/decision/caption behavior. The harness uses seeded SQLite state, normalized generated identifiers, golden payload assertions, and DB side-effect counts. Verified with focused characterization tests, full Campaign Factory pytest, contracts, architecture, artifact, integration, secret-scan, and repo test gates. No production code moves were included. |
| PR 1 — `CoreServices` extraction | Merged in PR #89 (`6d633ba`) | Extracts shared repository utilities into `campaign_factory.services.CoreServices` and keeps `CampaignFactory` as the public facade for `record_event`, `campaign_by_slug`, `graph_id_for`, `rendered_asset`, `ensure_graph_edge`, and the supporting `ensure_graph_node`. Verified with focused extraction facade tests, focused characterization tests, full Campaign Factory pytest (452 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 2 — Graph repository extraction | Merged in PR #90 (`cbc79b2`) | Extracts graph node/edge/sync-state persistence into `campaign_factory.graph.GraphRepository`, composed by `CoreServices` while keeping `CampaignFactory` public graph methods unchanged. Verified with focused extraction facade tests, focused characterization tests, full Campaign Factory pytest (453 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 3 — Event/job repository extraction | Merged in PR #92 (`450245a`) | Extracts activity-event and pipeline-job persistence into `campaign_factory.events.EventRepository`, composed by `CoreServices` while keeping `CampaignFactory` public event/job methods unchanged. Verified with focused extraction facade tests, focused characterization tests, full Campaign Factory pytest (454 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 4 — Model/account repository extraction | Merged in PR #93 (`166afce`) | Extracts model, campaign, account, and model-account-profile persistence into `campaign_factory.models.ModelRepository`, composed by `CoreServices` while keeping `CampaignFactory` public model/account methods unchanged. Verified with focused extraction facade tests, focused characterization tests, full Campaign Factory pytest (455 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 5 — Asset import repository extraction | Merged in PR #94 (`a673b8c`) | Extracts folder import and source-asset listing into `campaign_factory.asset_import.AssetImportRepository`, composed by `CoreServices` while keeping `CampaignFactory.import_folder` and `CampaignFactory.assets_for_campaign` signatures unchanged. Verified with focused extraction facade tests (9 passed), focused characterization tests (2 passed), full Campaign Factory pytest (456 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 6 — Creative planning repository extraction | Merged in PR #95 (`ce47d2c`) | Extracts creative-plan creation/status/progress/payload helpers into `campaign_factory.creative_planning.CreativePlanningRepository`, composed by `CoreServices` while keeping the public `CampaignFactory` creative-plan facade and private compatibility helpers unchanged. Verified with focused extraction facade tests (10 passed), focused characterization tests (2 passed), full Campaign Factory pytest (457 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 7 — Reference repository extraction | Merged in PR #96 (`400bed6`) | Extracts reference-bank import, prompt-pack lookup, pattern selection/plans, prepare-from-reference, active pattern lookup, and schedule-safe hook helpers into `campaign_factory.reference.ReferenceRepository`, composed by `CoreServices` while keeping the public `CampaignFactory` reference facade and private compatibility helpers unchanged. Verified with focused extraction facade tests (11 passed), focused reference behavior tests (2 passed), focused characterization tests (2 passed), full Campaign Factory pytest (458 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures` (rerun after an initial parallel `pnpm install` race), `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 8 — Caption family repository extraction | Merged in PR #101 (`d711d1b`) | Extracts caption-family planning/creation, planned caption-version construction, hashtag normalization, and caption-version payload lookup into `campaign_factory.caption.CaptionFamilyRepository`, composed by `CoreServices` while keeping the public `CampaignFactory` caption-family facade and private compatibility helpers unchanged. Verified with focused extraction facade tests (12 passed), focused caption behavior tests (7 passed, 391 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (459 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 9 — Distribution repository extraction | Merged in PR #103 (`70a1765`) | Extracts distribution-plan creation/listing/clearing, distribution-plan payloads, trial-heavy distribution planning, slot/account selection helpers, campaign distribution summaries, and latest-plan lookup into `campaign_factory.distribution.DistributionRepository`, composed by `CoreServices` while keeping the public `CampaignFactory` distribution facade and private compatibility helpers unchanged. Verified with focused extraction facade tests (14 passed), focused distribution behavior tests (7 passed, 391 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (461 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 10 — Decision ledger repository extraction | Merged in PR #104 (`cabffcd`) | Extracts decision-ledger preview/report/summary/filter helpers, decision entry construction, decision dedupe, supported-type metadata, obligation reason/explanation helpers, and story-goal mapping into `campaign_factory.decision_ledger.DecisionLedgerRepository`, composed by `CoreServices` while keeping the public `CampaignFactory` decision-ledger facade and private compatibility helpers unchanged. Verified with a RED missing-module failure first, focused extraction facade tests (15 passed), focused decision-ledger behavior tests (4 passed, 394 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (462 passed, 1 existing Starlette/httpx warning), `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. |
| PR 11 — Exception repository extraction | Merged in PR #107 (`047bdad`) | Extracts persisted trust-exception creation, lookup, filtering, trust summary, status transitions, and payload construction into `campaign_factory.exceptions.ExceptionRepository`, composed by `CoreServices` while keeping the public `CampaignFactory` exception facade and private compatibility helpers unchanged. Verified with a RED missing-module failure first, focused extraction facade tests (17 passed), focused persisted-exception behavior tests (2 passed, 396 deselected, 1 existing Starlette/httpx warning), focused queue-adjacent behavior tests (3 passed, 395 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (464 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 12 — Discoverability repository extraction | Merged in PR #113 (`a24c41d`) | Extracts discoverability safe-content contract, intake/generation/pre-render gates, violation origin mapping, prevention reports, discoverability loss/waterfall reporting, and captured/observed evidence helpers into `campaign_factory.discoverability.DiscoverabilityRepository`, composed by `CoreServices` while keeping the public `CampaignFactory` discoverability facade and private compatibility helpers unchanged. Verified with a RED missing-module failure first, focused extraction facade tests (19 passed), focused discoverability behavior tests (7 passed, 391 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (466 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 13 — Surface registration repository extraction | Merged in PR #116 (`70df747`) | Extracts surface-asset registration, component probing, staged-file placement, Instagram media-type mapping, and surface aspect-ratio validation into `campaign_factory.surface_registration.SurfaceRegistrationRepository`, composed by `CoreServices` while keeping the public `CampaignFactory.register_surface_asset` facade and private compatibility helpers unchanged. Carousel integrity stayed deferred to its own Tier 3 slice because it spans handoff/draft/proof boundaries. Verified with a RED missing-module failure first, focused extraction facade tests (21 passed), focused surface/carousel-adjacent behavior tests (12 passed, 386 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (468 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 14 — Carousel integrity repository extraction | Merged in PR #118 (`1bb3f49`) | Extracts carousel integrity reports, child metrics plans, carousel report asset filtering, slide signature comparison, boundary preservation checks, and Meta child payload previews into `campaign_factory.carousel_integrity.CarouselIntegrityRepository`, composed by `CoreServices` while keeping the public `CampaignFactory` carousel facades and private compatibility helpers unchanged. Surface handoff readiness and draft proof remain in `core.py` and are injected as callables. Verified with a RED missing-module failure first, focused extraction facade tests (23 passed), focused carousel behavior tests (9 passed, 389 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (470 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 15 — Winner expansion repository extraction | Merged in PR #119 (`d4f3f6f`) | Extracts winner expansion planning/reporting, variant-candidate scoring/decision helpers, latest variant audit lookup, variant inventory ranking/risk helpers, and variant-asset payload conversion into `campaign_factory.winner_expansion.WinnerExpansionRepository`, composed by `CoreServices` while keeping the public `CampaignFactory` winner-expansion facades and private compatibility helpers unchanged. `variant_inventory_plan` remains in `core.py` for this slice and uses the moved winner helpers through the service seam. Verified locally with a RED missing-module failure first, focused extraction facade tests (25 passed), focused winner/variant-inventory behavior tests (5 passed, 393 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (472 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 16 — Creative knowledge winner registry extraction | Merged in PR #120 (`72f3467`) | Extracts winner registry, concept registry, winner pattern rollups, winner knowledge base, winner memory item construction, concept-name extraction, posting-window labeling, and winner pattern grouping into `campaign_factory.creative_knowledge.CreativeKnowledgeRepository`, composed by `CoreServices` while keeping public `CampaignFactory` facades and private compatibility helpers unchanged. The lower-level creative knowledge builder stays in `core.py` for this slice and is injected as a dynamic callable. Verified locally with a RED missing-module failure first, focused extraction facade tests (27 passed), focused winner registry/pattern behavior tests (2 passed, 396 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), the monkeypatch compatibility regression test (1 passed, 1 existing Starlette/httpx warning), full Campaign Factory pytest (474 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 17 — TribeV2 A/B repository extraction | Merged in PR #121 (`4eaa649`) | Extracts advisory-only TRIBE v2 reel analysis, review, holdout pilot review, contact-sheet helpers, score lookup, correlation, bucket, metric-quality, and confidence helpers into `campaign_factory.tribev2.TribeV2Repository`, composed by `CoreServices` while keeping public `CampaignFactory` facades and private compatibility helpers unchanged. Creative-knowledge row/result access stays injected as dynamic callables. Verified locally with a RED missing-module failure first, focused extraction facade tests (29 passed), focused TRIBE v2 behavior tests (7 passed, 391 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (476 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 18 — Operator review repository extraction | Merged in PR #122 (`e4c73b3`) | Extracts operator inventory review batch planning, summaries, simulator reports, scenario helpers, batch-type ranking, candidate eligibility, candidate row construction, and recommended review actions into `campaign_factory.operator_review.OperatorReviewRepository`, composed by `CoreServices` while keeping public `CampaignFactory` facades and private compatibility helpers unchanged. Multi-blocker inventory unlock remains in `core.py` and is injected as a dynamic callable. Verified locally with a RED missing-module failure first, focused extraction facade tests (31 passed), focused operator-review behavior tests (5 passed, 398 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (486 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. |
| PR 19 — Story management repository extraction | Merged in PR #124 (`537bcfc`) | Extracts story inventory, intent, mix/calendar, quality-gate/OCR helpers, account story status/gap/summary reports, story certification proof, story production readiness, and story source-lineage blockers into `campaign_factory.story_management.StoryManagementRepository`, composed by `CoreServices` while keeping public `CampaignFactory` story facades and private compatibility helpers unchanged. Surface certification helpers, account/content needs, media probing, and read-only inventory helpers stay injected as dynamic callables to preserve monkeypatch compatibility and avoid back-imports. Verified locally with a RED missing-module failure first, focused extraction facade tests (32 passed), focused story behavior tests (30 passed, 373 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (487 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration` (rerun after an initial parallel `pnpm install` race), `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. |
| PR 20 — Account health repository extraction | Merged in PR #126 (`47296bf`) | Extracts account health/tier reports, restricted/manual/warmup account filters, readiness blockers/warnings, account trust/recommendation eligibility, maturity/warming/cadence, creative-risk, similarity-budget, and posting-guidance helpers into `campaign_factory.account_health.AccountHealthRepository`, composed by `CoreServices` while keeping public `CampaignFactory` facades and private compatibility helpers unchanged. Verified with a RED missing-module failure first, focused extraction facade tests (33 passed), focused account-health behavior tests (8 passed, 395 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (488 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 21,660 lines; the new account-health repository is 673 lines. |
| PR 21 — Autonomy policy repository extraction | Merged in PR #128 (`786988a`) | Extracts autonomy policy storage, validation, and policy payload construction into `campaign_factory.autonomy.AutonomyPolicyRepository`, composed by `CoreServices` while keeping `CampaignFactory.autonomy_level`, `CampaignFactory.set_autonomy_level`, and `CampaignFactory.autonomy_policy` signatures unchanged. `ExceptionRepository` receives the repository-backed autonomy-level callable through `CoreServices`. Verified with a RED missing-module failure first, focused extraction facade tests (34 passed), focused autonomy/trust/accepted-recommendation behavior tests (2 passed, 401 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (489 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. `core.py` was reduced to 21,637 lines; the new autonomy repository is 53 lines. |
| PR 22 — Account memory repository extraction | Merged in PR #130 (`5552cac`) | Extracts account-memory rebuild/reporting, account-memory payload lookup, pattern-stat aggregation, posting-window aggregation, fatigue scoring, recommendation-outcome summaries, and confidence scoring into `campaign_factory.account_memory.AccountMemoryRepository`, composed by `CoreServices` while keeping public `CampaignFactory.rebuild_account_memory` and `CampaignFactory.account_memory` signatures plus private compatibility helpers unchanged. Verified with a RED missing-module failure first, focused extraction facade tests (35 passed), focused characterization tests (2 passed), focused account-memory/account-fit behavior tests (1 passed, 402 deselected, 1 existing Starlette/httpx warning), full Campaign Factory pytest (490 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 21,341 lines; the new account-memory repository is 393 lines. |
| PR 23 — Recommendation accuracy repository extraction | Merged in PR #132 (`7d8d79c`) | Extracts recommendation-accuracy rebuild/reporting, observation upsert/querying, report persistence, accuracy segmentation, drift/trust scoring, audio-match evidence, outcome snapshot linking, and datetime parsing into `campaign_factory.recommendation_accuracy.RecommendationAccuracyRepository`, composed by `CoreServices` while keeping public `CampaignFactory.recommendation_accuracy` and `CampaignFactory.rebuild_recommendation_accuracy` signatures plus private compatibility helpers unchanged. Verified with a RED missing-module failure first, focused extraction facade tests (36 passed), focused recommendation-accuracy/trust behavior tests (2 passed, 401 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (491 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 20,934 lines; the new recommendation-accuracy repository is 568 lines. |
| PR 24 — Campaign overview repository extraction | Merged in PR #134 (`d794f9f`) | Extracts campaign health, asset detail, account assignment writes/lookups, and unresolved failed-job filtering into `campaign_factory.campaign_overview.CampaignOverviewRepository`, composed by `CoreServices` while keeping public `CampaignFactory.campaign_health`, `CampaignFactory.asset_detail`, `CampaignFactory.assign_asset_account`, `CampaignFactory.assignments_for_asset`, and `CampaignFactory.assignments_for_campaign` signatures plus private compatibility helper `_unresolved_failed_jobs` unchanged. Verified with a RED missing-module failure first, focused extraction facade tests (38 passed), focused campaign health/detail/assignment behavior tests (1 passed, 402 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (493 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 20,809 lines; the new campaign-overview repository is 205 lines. |
| PR 25 — Event job listing extraction | Merged in PR #136 (`78e2df1`) | Moves the remaining campaign job listing query into `campaign_factory.events.EventRepository`, delegates through `CoreServices`, and keeps public `CampaignFactory.jobs_for_campaign` unchanged. The campaign overview repository now receives the EventRepository-backed service delegate instead of a temporary `CampaignFactory` callback. Verified with a RED delegation failure first, focused extraction facade tests for `jobs_for_campaign` (2 passed), focused activity/jobs behavior tests (1 passed, 402 deselected, 1 existing Starlette/httpx warning), focused extraction facade tests (40 passed), focused characterization tests (2 passed), full Campaign Factory pytest (495 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 20,803 lines; `EventRepository` is 196 lines. |
| PR 26 — Surface summary repository extraction | Merged in PR #138 (`1fa2706`) | Extracts Creator OS surface summary, account surface summary, and creator surface gap wrapper reporting into `campaign_factory.surface_summary.SurfaceSummaryRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_surface_summary`, `CampaignFactory.account_surface_summary`, and `CampaignFactory.creator_surface_gap_report` signatures unchanged. Verified with a RED missing-module failure first, focused surface-summary extraction facade tests (3 passed), existing surface-summary behavior/CLI tests (2 passed, 1 existing Starlette/httpx warning), focused extraction facade tests (42 passed), focused characterization tests (2 passed), full Campaign Factory pytest (497 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 20,782 lines; the new surface-summary repository is 89 lines. |
| PR 27 — Draft inventory gap repository extraction | Merged in PR #140 (`46091c3`) | Extracts Creator OS draft inventory gap reporting into `campaign_factory.draft_inventory_gap.DraftInventoryGapRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_os_draft_inventory_gap` unchanged. Verified with a RED missing-module failure first, focused draft-inventory-gap extraction facade tests (3 passed), existing draft-inventory-gap behavior/CLI-adjacent tests (5 passed, 398 deselected, 1 existing Starlette/httpx warning), focused extraction facade tests (44 passed), focused characterization tests (2 passed), full Campaign Factory pytest (499 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 20,706 lines; the new draft-inventory-gap repository is 128 lines. |
| PR 28 — Daily plan repository extraction | Merged in PR #142 (`6fbd7e2`) | Extracts Creator OS daily planning into `campaign_factory.daily_plan.DailyPlanRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_os_daily_plan` unchanged. Verified with a RED missing-module failure first, focused daily-plan extraction facade tests (3 passed), existing daily-plan behavior tests (17 passed, 386 deselected, 1 existing Starlette/httpx warning), readiness/acceptance callers (9 passed, 394 deselected, 1 existing Starlette/httpx warning), focused extraction facade tests (46 passed), focused characterization tests (2 passed), full Campaign Factory pytest (501 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. `core.py` was reduced to 20,484 lines; the new daily-plan repository is 324 lines. |
| PR 29 — Execution readiness repository extraction | Merged in PR #144 (`13be0ed`) | Extracts Creator OS execution readiness into `campaign_factory.execution_readiness.ExecutionReadinessRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_os_execution_readiness` unchanged. The legacy `campaign_factory.readiness.creator_os_execution_readiness` module function remains as a compatibility shim that delegates to the repository. Verified with a RED missing-module failure first, focused execution-readiness extraction facade tests (3 passed), existing execution-readiness/readiness-acceptance behavior tests (9 passed, 394 deselected, 1 existing Starlette/httpx warning), focused extraction facade tests (47 passed), focused characterization tests (2 passed), full Campaign Factory pytest (502 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. `core.py` was reduced to 20,446 lines; the new execution-readiness repository is 219 lines. |
| PR 30 — 200-account acceptance suite extraction | Merged in PR #146 (`074090d`) | Extracts the synthetic 200-account operational acceptance suite and its synthetic account/draft/report helpers into `campaign_factory.acceptance_suite.AcceptanceSuiteRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_os_200_account_acceptance_suite` unchanged. Verified with a RED missing-module failure first, focused acceptance-suite extraction facade tests (3 passed), existing 200-account acceptance behavior test (1 passed, 402 deselected, 1 existing Starlette/httpx warning), focused extraction facade tests (49 passed), focused characterization tests (2 passed), full Campaign Factory pytest (504 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI after rebasing onto `origin/main`. `core.py` was reduced to 20,170 lines; the new acceptance-suite repository is 325 lines. |
| PR 31 — Synthetic readiness report extraction | Merged in PR #148 (`5be033e`) | Extracts read-only synthetic readiness reporting into `campaign_factory.readiness_report.ReadinessReportRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_os_100_account_proof`, `CampaignFactory.creator_os_volume_acceptance_suite`, `CampaignFactory.surface_readiness_scorecard`, `CampaignFactory.creator_os_10_0_readiness_report`, and `CampaignFactory.creator_os_9_5_readiness_report` unchanged. The existing mass-production readiness report function remains in the same module, with its `CampaignFactory` dependency moved behind `TYPE_CHECKING` to avoid a service import cycle. Verified with a RED missing-repository import failure first, focused readiness-report extraction facade tests (3 passed), existing readiness-report behavior/CLI tests (3 passed, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (51 passed), full Campaign Factory pytest (506 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI after rebasing onto `origin/main`. `core.py` was reduced to 20,006 lines; `readiness_report.py` is 969 lines. |
| PR 32 — Live scale report extraction | Merged in PR #150 (`b13080b`) | Extracts read-only live-scale runbook and scorecard reporting into `campaign_factory.live_scale.LiveScaleRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_os_live_scale_runbook` and `CampaignFactory.creator_os_live_scale_scorecard` unchanged. Live 100-account readiness remains in `core.py` for a separate slice and is injected as a callable. Verified with a RED missing-module failure first, focused live-scale extraction facade tests (3 passed), existing final-certification behavior test covering live readiness/runbook/scorecard (1 passed, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (53 passed), full Campaign Factory pytest (508 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI after rebasing onto `origin/main`. `core.py` was reduced to 19,984 lines; the new live-scale repository is 62 lines. |
| PR 33 — Live 100-account readiness extraction | Merged in PR #152 (`8573313`) | Moves live 100-account readiness, actual account operational counts, and exact-shortfall construction into `campaign_factory.live_scale.LiveScaleRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_os_live_100_account_readiness` and private compatibility helpers unchanged. Live-scale runbook/scorecard now call the repository-owned readiness method instead of a facade callback. Verified with RED delegation failures first, focused live-scale extraction facade tests (3 passed), existing final-certification behavior test covering live readiness/runbook/scorecard (1 passed, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (53 passed), full Campaign Factory pytest (508 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 19,920 lines; `live_scale.py` is 168 lines. |
| PR 34 — Live account acceptance extraction | Merged in PR #153 (`1864da9`) | Moves live account acceptance, staged live acceptance, live acceptance actuals, and live acceptance evidence helpers into `campaign_factory.live_acceptance.LiveAcceptanceRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_os_live_account_acceptance`, `CampaignFactory.creator_os_staged_live_acceptance`, and private compatibility helpers unchanged. Reservation-adjusted inventory and surface readiness remain injected callables for later inventory/surface slices. Verified with a RED missing-module failure first, focused live-account-acceptance extraction facade tests (3 passed), existing live-account/staged acceptance behavior tests (5 passed, 398 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (55 passed), full Campaign Factory pytest (510 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 19,806 lines; `live_acceptance.py` is 209 lines. |
| PR 35 — Certification report extraction | Merged in PR #155 (`8bce293`) | Moves the Creator OS certification report aggregator into `campaign_factory.certification.CertificationRepository`, composed by `CoreServices` while keeping public `CampaignFactory.creator_os_certification_report` unchanged. The repository consumes existing live-readiness, parent-factory scorecard, discoverability scorecard, story proof, and carousel proof callables without moving those underlying domains. Verified with a RED missing-module failure first, focused certification extraction facade tests (3 passed), existing final-certification behavior test (1 passed, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (57 passed), full Campaign Factory pytest (512 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 19,759 lines; `certification.py` is 75 lines. |
| PR 36 — Operational proof extraction | Merged in PR #157 (`ee64fc2`) | Moves read-only failure-injection, idempotency, surface-maturity, operator-load, and idempotency-evidence proof helpers into `campaign_factory.operational_proofs.OperationalProofRepository`, composed by `CoreServices` while keeping public `CampaignFactory.failure_injection_suite`, `CampaignFactory.idempotency_proof`, `CampaignFactory.surface_maturity_audit`, `CampaignFactory.operator_load_audit`, and private compatibility helper `_idempotency_evidence_for_path` unchanged. Readiness-report and live-scale repositories now consume repository-owned proof callables instead of facade callbacks. Verified with a RED missing-module failure first, focused operational-proof extraction facade tests (3 passed), existing operational-proof/readiness behavior tests (2 passed, 401 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (59 passed), full Campaign Factory pytest (514 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI after rebasing onto `origin/main`. `core.py` was reduced to 19,633 lines; `operational_proofs.py` is 148 lines. |
| PR 37 — Core complexity report extraction | Merged in PR #158 (`519e7a5`) | Moves read-only single-source-of-truth and core-complexity reduction reports plus largest-project-file inspection into `campaign_factory.core_complexity.CoreComplexityRepository`, composed by `CoreServices` while keeping public `CampaignFactory.single_source_of_truth_audit`, `CampaignFactory.core_complexity_reduction_plan`, and private compatibility helper `_largest_project_files` unchanged. Verified with a RED missing-module failure first, focused core-complexity extraction facade tests (3 passed), existing ownership/complexity/readiness behavior test (1 passed, 402 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (61 passed), full Campaign Factory pytest (516 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 19,561 lines; `core_complexity.py` is 93 lines. |
| PR 38 — Carousel certification/readiness extraction | Merged in PR #160 (`6aba39c`) | Moves carousel certification proof, carousel production readiness, carousel proof-gap analysis, certification-asset lookup, latest proof/metric lookup, and surface certification audit payload helpers into `campaign_factory.carousel_integrity.CarouselIntegrityRepository`, composed by `CoreServices` while keeping public `CampaignFactory.carousel_certification_proof`, `CampaignFactory.carousel_production_readiness`, `CampaignFactory.carousel_proof_gap_analysis`, and private compatibility helpers unchanged. Verified with a RED delegation failure first, focused carousel extraction facade tests (2 passed), existing carousel/final-certification behavior tests (3 passed, 400 deselected), focused characterization tests (2 passed), focused extraction facade tests (61 passed), full Campaign Factory pytest (516 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 19,388 lines; `carousel_integrity.py` is 514 lines. |
| PR 39 — Surface handoff/draft proof extraction | Merged in PR #161 (`7d5d01b`) | Moves surface handoff readiness report, surface draft proof, surface asset listing, surface readiness list building, and surface draft payload construction into `campaign_factory.surface_handoff.SurfaceHandoffRepository`, composed by `CoreServices` while keeping public `CampaignFactory.surface_handoff_readiness_report`, `CampaignFactory.surface_draft_proof`, and private compatibility helpers unchanged. The repository uses injected facade callables for report assembly so existing monkeypatch characterization still observes `_build_surface_readiness`. Verified with a RED missing-module failure first, focused surface-handoff extraction facade tests (2 passed), existing surface handoff/draft/phase2 behavior tests (9 passed, 394 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (63 passed), full Campaign Factory pytest (518 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. `core.py` was reduced to 19,306 lines; `surface_handoff.py` was 151 lines. |
| PR 40 — Surface handoff readiness internals extraction | Merged in PR #163 (`f6ca160`) | Moves per-asset surface handoff readiness, content-trust status blockers, operator visual-review gating, creator matching, asset component lookup, Instagram media-type mapping, handoff aspect-ratio validation, and blank-caption allowance checks into `campaign_factory.surface_handoff.SurfaceHandoffRepository`, composed by `CoreServices` while keeping public and private `CampaignFactory` compatibility helpers unchanged. Verified with RED facade/service delegation failures first, focused surface-handoff extraction facade tests (2 passed), existing surface handoff/draft/phase2/operator-review behavior tests (11 passed, 392 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (63 passed), full Campaign Factory pytest (518 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. `core.py` is now 19,018 lines; `surface_handoff.py` is 501 lines. |
| PR 41 — Surface requirements repository extraction | Merged in PR #165 (`42c534d`) | Moves account surface obligations, account/creator content-needs reports, surface status, surface gap reporting, requirement-row lookup, content-obligation construction, surface totals, requirement cadence checks, schedule/completion counts, and last-posted lookup into `campaign_factory.surface_requirements.SurfaceRequirementsRepository`, composed by `CoreServices` while keeping public and private `CampaignFactory` compatibility helpers unchanged. Downstream decision-ledger, story-management, and surface-summary repositories now receive repository-owned content-needs helpers instead of facade callbacks. Verified with a RED missing-module failure first, focused surface-requirements extraction facade tests (2 passed), existing surface obligation/content-needs/gap behavior tests (6 passed, 397 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (65 passed), full Campaign Factory pytest (520 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. `core.py` is now 18,688 lines; `surface_requirements.py` is 457 lines. |
| PR 42 — Surface inventory repository extraction | Merged in PR #167 (`fb45c5e`) | Moves multi-surface inventory audit and surface inventory building into `campaign_factory.surface_inventory.SurfaceInventoryRepository`, composed by `CoreServices` while keeping public `CampaignFactory.multi_surface_inventory_audit` and private `_build_surface_inventory` compatibility helpers unchanged. Surface requirements, story management, and surface summary now consume the same inventory seam; late-bound facade callbacks preserve existing monkeypatch characterization for phase-2 helper sharing. Verified with a RED missing-module failure first, focused surface-inventory extraction facade tests (2 passed), existing multi-surface inventory/surface-gap behavior tests (4 passed, 399 deselected, 1 existing Starlette/httpx warning), phase-2 helper-sharing regression test (1 passed, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (67 passed), full Campaign Factory pytest (522 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI after rebasing onto `origin/main`. `core.py` is now 18,648 lines; `surface_inventory.py` is 83 lines. |
| PR 43 — Exception queue repository extraction | Merged in PR #169 (`2a3cf3f`) | Moves exception queue reporting, summary/priority/owner rollups, queue item construction, severity/action/category/owner/repairability classification, and estimated resolution minutes into `campaign_factory.exceptions.ExceptionRepository`, composed by `CoreServices` while keeping public `CampaignFactory.exception_queue_report`, `exception_queue_summary`, `exception_queue_priority_report`, `exception_queue_owner_report`, and private compatibility helpers unchanged. Verified with RED delegation failures first, focused exception extraction facade tests (2 passed), existing exception queue and inventory-adjacent behavior tests (3 passed, 400 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (67 passed), full Campaign Factory pytest (522 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI after rebasing onto `origin/main`. `core.py` is now 18,443 lines; `exceptions.py` is 591 lines. |
| PR 44 — Finished-video intake repository extraction | Merged in PR #171 (`249e45c`) | Moves finished-video intake orchestration, native hook generation, preflight warning construction, style-lane format mapping, caption band/font selection, and filename-based format classification into `campaign_factory.finished_video.FinishedVideoRepository`, composed by `CoreServices` while keeping public `CampaignFactory.intake_finished_video`, `finished_video_hooks`, and private compatibility helpers unchanged. Existing monkeypatch behavior is preserved through late-bound `make_batch`, lineage, and probe callables. Verified with a RED missing-module failure first, focused finished-video extraction facade tests (2 passed), existing finished-video behavior tests (8 passed, 395 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (69 passed), full Campaign Factory pytest (524 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI after rebasing onto `origin/main`. `core.py` is now 18,249 lines; `finished_video.py` is 298 lines. |
| PR 45 — Finished-video registration/review extraction | Merged in PR #172 (`8aaafdb`) | Moves rendered-asset review/approval plus operator-approved finished-video registration and operator-audit payload persistence into `campaign_factory.finished_video.FinishedVideoRepository`, composed by `CoreServices` while keeping public `CampaignFactory.review_rendered_asset`, `approve_rendered_asset`, and `register_finished_video` signatures unchanged. Discoverability rejection evidence and publishability rejection capture remain late-bound facade callbacks to avoid importing `CampaignFactory`. Verified with RED delegation failures first, focused finished-video extraction facade tests (4 passed), focused finished-video registration/review behavior tests (5 passed, 398 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), focused extraction facade tests (71 passed), full Campaign Factory pytest (526 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. `core.py` is now 17,893 lines; `finished_video.py` is 755 lines. |
| PR 46 — Reel execution repository extraction | Merged in PR #174 (`dcaf0b5`) | Moves reel input preparation, Reel Factory subprocess execution, reel output manifest sync, caption/lineage/audio-intent sync helpers, and reel clip numbering/hook sidecar helpers into `campaign_factory.reel_execution.ReelExecutionRepository`, composed by `CoreServices` while keeping public `CampaignFactory.prepare_reel_inputs`, `run_reel_factory`, `sync_reel_outputs`, and private compatibility helpers unchanged. `CampaignFactory` remains the monkeypatch-compatible facade used by `make_batch`, CLI, API, and adjacent stages. Verified with RED missing-module/delegation failure first, focused reel-execution extraction facade tests (2 passed), focused reel behavior tests (9 passed, 394 deselected, 1 existing Starlette/httpx warning), focused extraction facade tests (73 passed), focused characterization tests (2 passed), full Campaign Factory pytest (528 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. After rebasing onto latest `origin/main`, reran the focused reel-execution facade and reel behavior tests. `core.py` is now 17,196 lines; `reel_execution.py` is 929 lines. |
| PR 47 — Variant lineage repository extraction | Merged in PR #176 (`256419d`) | Moves parent/variant registration, ContentForge variant-pack generation, variant inventory, variant metrics rollup, and concept/variant lineage payload helpers into `campaign_factory.variant_lineage.VariantLineageRepository`, composed by `CoreServices` while keeping public `CampaignFactory.register_parent_reel`, `variant_plan`, `generate_variants`, `register_variant_asset`, `parent_variant_inventory`, `variant_metrics_rollup`, and private compatibility helpers unchanged. Existing `urlopen` and `register_variant_asset` monkeypatch seams stay late-bound through injected callables. Verified with RED missing-module/delegation failure first, focused variant-lineage extraction facade tests (2 passed), focused variant behavior tests (8 passed, 395 deselected, 1 existing Starlette/httpx warning), focused extraction facade tests (75 passed), focused characterization tests (2 passed), rollback monkeypatch regression test (1 passed, 1 existing Starlette/httpx warning), full Campaign Factory pytest (530 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch` (rerun after an initial parallel fixture race), `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. `core.py` is now 16,362 lines; `variant_lineage.py` is 1,047 lines. |
| PR 48 — Publishability/proof repository extraction | Merged in PR #178 (`4c39898`) | Moves local export readiness, publishability explanation, rejection-evidence capture, discoverability rejection capture, proof-run persistence, publishability discoverability-field extraction, Instagram post-caption quality checks, caption-quality repair planning, caption repair classification, caption repair suggestion, and publishability checks into `campaign_factory.publishability.PublishabilityRepository`, composed by `CoreServices` while keeping public `CampaignFactory.explain_publishability`, `capture_publishability_rejection_evidence`, `record_proof_run`, `caption_quality_repair_plan`, and private compatibility helpers unchanged. Adjacent quarantine, audio, cover-frame, sidecar, and handoff helpers stay injected as callables for narrower future slices. Verified with RED missing-module failure first, focused publishability extraction facade tests (2 passed), focused extraction facade tests (77 passed), focused publishability behavior tests (21 passed, 382 deselected, 1 existing Starlette/httpx warning), focused characterization tests (2 passed), full Campaign Factory pytest (532 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. `core.py` is now 15,779 lines; `publishability.py` is 846 lines. |
| PR 49 — Recommendation execution/lifecycle repository extraction | Merged in PR #179 (`370c851`) | Moves recommendation next-batch generation, recommendation run listing, ranking/evidence compaction, recommendation item payload construction, graph-edge writes, accept/reject/link/measure lifecycle transitions, accepted-item execution, trust exception creation, baseline/performance scoring helpers, account-fit evidence, and hook/caption guidance into `campaign_factory.recommendations.RecommendationRepository`, composed by `CoreServices` while keeping public `CampaignFactory.recommend_next_batch`, `recommendation_runs`, `recommendation_item`, `accept_recommendation_item`, `reject_recommendation_item`, `link_recommendation_item`, `measure_recommendation_item`, `execute_accepted_recommendation`, and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 15,779 lines before the slice to 14,213 lines after extraction; `recommendations.py` is 1,974 lines. Local verification so far: expected RED missing-module failure before implementation, focused recommendation delegation tests (2 passed), focused recommendation behavior tests (8 passed, 395 deselected, 1 existing Starlette/httpx warning), focused extraction facade tests (79 passed), recommendation-accuracy-adjacent test (1 passed, 402 deselected, 1 existing Starlette/httpx warning), full Campaign Factory pytest (534 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 50 — Archive inventory/candidate-quality repository extraction | Merged in PR #182 (`553c6ed`) | Moves archive inventory reporting, duplicate detection helpers, candidate quality ranking, crop severity scoring, visual quality scoring, and duplicate-confidence classification into `campaign_factory.archive_quality.ArchiveQualityRepository`, composed by `CoreServices` while keeping public `CampaignFactory.archive_inventory_report`, `archive_candidate_quality_report`, and private compatibility helpers unchanged. The existing `campaign_factory.core.probe_video_metadata` monkeypatch seam stays late-bound through an injected callable. Net-shrink invariant: `core.py` reduced from 14,213 lines before the slice to 13,881 lines after extraction; `archive_quality.py` is 414 lines. Verified with expected RED missing-module failure before implementation, focused archive-quality delegation test (1 passed, 79 deselected), focused archive behavior tests (6 passed, 397 deselected, 1 existing Starlette/httpx warning), focused extraction facade tests (80 passed), full Campaign Factory pytest (535 passed, 1 existing Starlette/httpx warning), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 51 — Inventory Tier-3 characterization coverage gate | Merged in PR #184 (`737333a`) | Added tests-only characterization coverage before extracting inventory, because the Tier-3 gate found no existing inventory coverage in `test_core_characterization.py`. The tests pin inventory planning/reporting public methods as read-only and reservation/net-inventory behavior across idempotent reservations, assigned inventory, release, and live-account acceptance. No production code moves were included; `core.py` remained 13,881 lines. Verified with focused inventory characterization tests, full characterization tests, full Campaign Factory pytest, repo gates, and green PR CI. |
| PR 52 — Inventory planning/reporting repository extraction | Merged in PR #186 (`62dde6e`) | Moves read-only inventory planning/reporting methods plus inventory-specific helper compatibility methods into `campaign_factory.inventory_planning.InventoryPlanningRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures unchanged. Net-shrink invariant: `core.py` reduced from 13,881 lines before the slice to 13,510 lines after extraction; `inventory_planning.py` is 607 lines. Verified with focused inventory delegation tests, inventory characterization tests, focused inventory behavior tests, full extraction facade tests, full Campaign Factory pytest, repo gates, and green PR CI. |
| PR 53 — Inventory reservation/net-inventory repository extraction | Merged in PR #190 (`67017ea`) | Moves inventory reservation writes, reservation expiry/release, uniqueness cooldown checks, and reservation-adjusted net-inventory calculations into `campaign_factory.inventory_reservations.InventoryReservationRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Perceptual metadata generation remained in `core.py` for a separate slice and was injected as a callable. Net-shrink invariant: `core.py` reduced from 13,510 lines before the slice to 13,303 lines after extraction; `inventory_reservations.py` is 300 lines. Verified with expected RED missing-module failure before implementation, focused inventory-reservation delegation tests (3 passed), focused reservation/net-inventory behavior tests (7 passed, 396 deselected), inventory characterization tests (2 passed, 2 deselected), full extraction facade tests (84 passed), full Campaign Factory pytest (541 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI after rebasing onto current `origin/main`. |
| PR 54 — Inventory perceptual metadata repository extraction | Merged in PR #194 (`25e2a76`) | Moves asset uniqueness extraction, rendered-asset perceptual metadata persistence, and PDQ cluster matching into `campaign_factory.inventory_perceptual.InventoryPerceptualRepository`, composed by `CoreServices` while keeping public `CampaignFactory.ensure_rendered_asset_perceptual_metadata` and private compatibility helpers unchanged. `InventoryReservationRepository` now consumes the perceptual repository through the service seam, and the existing `campaign_factory.core.compute_pdq_fingerprint` monkeypatch compatibility is preserved through late-bound detector callables. Net-shrink invariant: `core.py` reduced from 13,303 lines before the slice to 13,200 lines after extraction; `inventory_perceptual.py` is 158 lines. Verified with expected RED missing-module failure before implementation, focused inventory-perceptual delegation tests (3 passed), focused reservation/net-inventory/PDQ behavior tests (7 passed, 396 deselected, 1 existing Starlette/httpx warning), full extraction facade tests (86 passed), full Campaign Factory pytest (543 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI after rebasing onto current `origin/main`. |
| PR 55 — Inventory recovery repository extraction | Merged in PR #195 (`1eb2f20`) | Moves inventory recovery reports, recovery priority/by-blocker/master projections, blocked-asset classification, repair-class rows, recovery-unlock counting, and ROI priority sorting into `campaign_factory.inventory_recovery.InventoryRecoveryRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Schedule-safe production, ContentForge QC, multi-blocker unlock, and operator-review methods stayed in their existing homes for narrower follow-up slices. Net-shrink invariant: `core.py` reduced from 13,200 lines before the slice to 13,017 lines after extraction; `inventory_recovery.py` is 247 lines. Verified with expected RED missing-module failure before implementation, focused inventory-recovery delegation tests (2 passed), focused inventory recovery behavior tests (2 passed, 401 deselected, 1 existing Starlette/httpx warning), inventory characterization tests (2 passed, 2 deselected), full extraction facade tests (88 passed), full Campaign Factory pytest (545 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 56 — Schedule-safe production repository extraction | Merged in PR #197 (`da6e640`) | Moves schedule-safe production reports, waterfall/loss/capacity/master projections, fresh production asset filtering, stage waterfall construction, variant readiness checks, capacity math, parent/variant requirement estimates, and summary-key mapping into `campaign_factory.schedule_safe_production.ScheduleSafeProductionRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Fresh-reel planning, ContentForge QC, throughput proof, multi-blocker unlock, and operator-review methods stay in their existing homes for narrower follow-up slices. Net-shrink invariant: `core.py` reduced from 13,017 lines before the slice to 12,800 lines after extraction; `schedule_safe_production.py` is 330 lines. Verified with expected RED missing-module failure before implementation, focused schedule-safe delegation tests (2 passed), focused schedule-safe behavior tests (3 passed, 400 deselected, 1 existing Starlette/httpx warning), full extraction facade tests (90 passed), inventory characterization tests (2 passed, 2 deselected), full Campaign Factory pytest (547 passed, 1 existing Starlette/httpx warning), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 57 — Make-batch/slideshow orchestration repository extraction | Merged in PR #198 (`5e63c4f`) | Moves make-batch orchestration, slideshow-pack rendering/registration, source media summary, and format selection into `campaign_factory.make_batch.MakeBatchRepository`, composed by `CoreServices` while keeping public `CampaignFactory.make_batch` and private compatibility helpers unchanged. Adapter-facing and monkeypatchable seams continue to route through the original factory context. Net-shrink invariant: `core.py` reduced from 12,800 lines before the slice to 12,324 lines after extraction; `make_batch.py` is 648 lines. Verified with expected RED missing-module failure before implementation, focused make-batch delegation tests (2 passed), focused make-batch/slideshow behavior tests (3 passed, 400 deselected), adjacent make-batch/finished-video/reel behavior tests (12 passed, 391 deselected), full extraction facade tests (92 passed), full Campaign Factory pytest (549 passed), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 58 — Fresh reel production planning repository extraction | Merged in PR #199 (`828f84a`) | Moves fresh schedule-safe reel production planning, reel batch/capacity/master projections, current schedule-safe reel inventory lookup, downstream yield math, expected stage rows/evidence, and execution-batch construction into `campaign_factory.fresh_reel_production.FreshReelProductionRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 12,324 lines before the slice to 12,135 lines after extraction; `fresh_reel_production.py` is 297 lines. Verified with expected RED missing-module failure before implementation, focused fresh-reel delegation tests (2 passed), focused fresh schedule-safe behavior/CLI tests (3 passed, 400 deselected), full extraction facade tests (94 passed), full Campaign Factory pytest (551 passed), repo gates, `pnpm graphify:update`, clean whitespace diff, and green PR CI. |
| PR 59 — ContentForge visual QC repository extraction | Merged in PR #200 (`a2edb9f`) | Moves ContentForge visual QC failure reporting, waterfall/loss/repair/master projections, visual failure classification, non-visual gate checks, category rows, recovered-inventory math, and answer text into `campaign_factory.contentforge_visual_qc.ContentForgeVisualQCRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 12,135 lines before the slice to 11,888 lines after extraction; `contentforge_visual_qc.py` is 345 lines. Verified with expected RED missing-module failure before implementation, focused ContentForge visual QC delegation tests (2 passed), focused ContentForge visual QC behavior/CLI tests (3 passed, 400 deselected), full extraction facade tests (96 passed), full Campaign Factory pytest (553 passed), repo gates, `pnpm graphify:update`, clean whitespace diff, and green PR CI. |
| PR 60 — Multi-blocker inventory unlock repository extraction | Merged in PR #201 (`3a01582`) | Moves multi-blocker inventory unlock reports/plans, repair-class classification, repair-combination rows, unlocked-asset/minutes/difficulty scoring, best-combo selection, and minimal-fix-set selection into `campaign_factory.multi_blocker_unlock.MultiBlockerUnlockRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 11,888 lines before the slice to 11,694 lines after extraction; `multi_blocker_unlock.py` is 288 lines. Verified with expected RED missing-module failure before implementation, focused multi-blocker delegation tests (2 passed), focused inventory unlock behavior tests (2 passed, 401 deselected), full extraction facade tests (98 passed), full Campaign Factory pytest (555 passed), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 61 — Lifecycle reporting repository extraction | Merged in PR #202 (`1f27e52`) | Moves campaign readiness, lifecycle report, Creator OS lifecycle dashboard, lifecycle bucket mapping, ThreadsDashboard post indexing, lifecycle row/state derivation, lineage/media mismatch helpers, past-due helpers, lifecycle fingerprints, and compact lifecycle evidence payload helpers into `campaign_factory.lifecycle_reporting.LifecycleReportingRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 11,694 lines before the slice to 11,294 lines after extraction; `lifecycle_reporting.py` is 602 lines. Verified with expected RED missing-module failure before implementation, focused lifecycle delegation tests (2 passed), focused lifecycle behavior/CLI/readiness tests (10 passed, 393 deselected), full extraction facade tests (100 passed), full Campaign Factory pytest (557 passed), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 62 — Performance summary repository extraction | Merged in PR #203 (`0320780`) | Moves read-only performance summary, caption-outcome report, per-asset performance payloads, grouping/aggregation facades, metric-contract defaults, leaderboards, caption-outcome manual review, performance dimensions, recommendation labels, and performance scoring helpers into `campaign_factory.performance_summary.PerformanceSummaryRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 11,294 lines before the slice to 10,752 lines after extraction; `performance_summary.py` is 665 lines. Verified with expected RED missing-module failure before implementation, focused performance delegation tests (2 passed), focused performance-summary/dashboard/API behavior tests (4 passed, 399 deselected), full extraction facade tests (102 passed), full Campaign Factory pytest (559 passed), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 63 — Audio recommendation/catalog repository extraction | Merged in PR #204 (`6008d9a`) | Moves audio catalog import/listing, audio memory import/listing, audio recommendation, audio decisioning, catalog scoring, performance/fatigue summaries, ContentForge audio-fit adapter calls, and audio helper payload construction into `campaign_factory.audio_recommendations.AudioRecommendationRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 10,752 lines before the slice to 9,981 lines after extraction; `audio_recommendations.py` is 1,007 lines. Verified with expected RED missing-module failure before implementation, focused audio delegation tests (2 passed), focused audio catalog/memory/decision/dashboard-intent behavior tests (7 passed, 396 deselected), full extraction facade tests (104 passed), full Campaign Factory pytest (561 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. |
| PR 64 — Audio operations/proof repository extraction | Merged in PR #205 (`06cb9a9`) | Moves campaign audio attachment, cover-frame attachment, audio selection, audio verification, audio performance rollup writes, audio workflow dashboard summaries, audio/cover-frame normalizers, and publishability audio-proof helpers into `campaign_factory.audio_operations.AudioOperationsRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 9,981 lines before the slice to 9,150 lines after extraction; `audio_operations.py` is 1,037 lines. Verified with expected RED missing-module failure before implementation, focused audio-operation delegation tests (2 passed), focused audio selection/attachment/verification/dashboard/publishability behavior tests (8 passed, 397 deselected), full extraction facade tests (106 passed), full Campaign Factory pytest (565 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 65 — Reel Factory report repository extraction | Merged in PR #206 (`2a21b01`) | Moves read-only Reel Factory throughput proof, yield/failure/capacity analysis, scale readiness, master report aggregation, parent metrics, QC pass, confidence, operator-minute, intake/creation/quality/readiness/human-cost helper calculations into `campaign_factory.reel_factory_reports.ReelFactoryReportRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 9,150 lines before the slice to 8,891 lines after extraction; `reel_factory_reports.py` is 339 lines. Verified with focused Reel Factory report delegation tests (2 passed), focused Reel Factory behavior/CLI tests (4 passed, 401 deselected), full extraction facade tests (108 passed), full Campaign Factory pytest (567 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 66 — Parent Factory report repository extraction | Merged in PR #207 (`49f80e3`) | Moves read-only Parent Factory yield waterfall, loss/rejection/quality/optimization/master reports, recoverable-yield/recovery/feasibility/secondary-loss/true-yield/realistic-plan calculations, and related report helper methods into `campaign_factory.parent_factory_reports.ParentFactoryReportRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. Production trial and post-gate fresh-batch proof stay in `core.py` for later slices. Net-shrink invariant: `core.py` reduced from 8,891 lines before the slice to 8,503 lines after extraction; `parent_factory_reports.py` is 477 lines. Verified with focused Parent Factory report delegation tests (2 passed), focused parent report behavior/CLI tests (10 passed, 395 deselected), full extraction facade tests (110 passed), full Campaign Factory pytest (569 passed), repo gates, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 67 — Parent Factory trial/proof repository extraction | Merged in PR #208 (`2c2dbac`) | Moves Parent Factory production trial, latest measured trial lookup, 53-parent trial/results/analysis, post-gate fresh-batch proof, production scorecard, real-yield report, and post-gate fixture/evidence helpers into `campaign_factory.parent_factory_trials.ParentFactoryTrialRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. The post-gate proof sandbox receives an injected `CampaignFactory` constructor to preserve the existing `self.__class__(Settings(...))` seam without back-importing the facade. Net-shrink invariant: `core.py` reduced from 8,503 lines before the slice to 8,171 lines after extraction; `parent_factory_trials.py` is 400 lines. Verified with focused Parent Factory report/trial delegation tests (4 passed), focused parent trial/proof behavior tests (4 passed, 401 deselected), full Campaign Factory pytest (571 passed), repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 68 — Parent Factory planning repository extraction | Merged in PR #209 (`e187a59`) | Moves Parent Factory autopilot, shortfall, and production-target planning into `campaign_factory.parent_factory_planning.ParentFactoryPlanningRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures unchanged. This deliberately leaves `_road_to_accounts_payload` in the facade because it is an inventory-planning helper injected earlier in service construction. Net-shrink invariant: `core.py` reduced from 8,171 lines before the slice to 8,136 lines after extraction; `parent_factory_planning.py` is 75 lines. Verified with focused Parent Factory planning delegation tests (2 passed), focused parent planning behavior tests (3 passed), full Campaign Factory pytest, repo gates, `pnpm graphify:update`, and green PR CI. |
| PR 69 — Variant inventory plan repository extraction | Merged in PR #210 (`08b5b50`) | Moves `variant_inventory_plan` into `campaign_factory.winner_expansion.WinnerExpansionRepository`, composed by `CoreServices` while keeping the public `CampaignFactory.variant_inventory_plan` signature unchanged. The repository receives injected creator-label and concept-payload callables so the existing facade compatibility seams stay stable. Net-shrink invariant: `core.py` reduced from 8,136 lines before the slice to 7,969 lines after extraction. Verified locally with syntax compile, winner-expansion delegation tests (2 passed), variant-inventory behavior tests (5 passed), full Campaign Factory pytest (573 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch` (rerun after a parallel fixture race), `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 70 — Recommended inventory request repository extraction | Merged in PR #211 (`3a8087a`) | Moves `recommended_inventory_request_plan` and its creator-row, existing-inventory, variant-batch, and action helpers into `campaign_factory.recommended_inventory_request.RecommendedInventoryRequestRepository`, composed by `CoreServices` while keeping the public `CampaignFactory.recommended_inventory_request_plan` signature and private compatibility helpers unchanged. Net-shrink invariant: `core.py` reduced from 7,969 lines before the slice to 7,877 lines after extraction; `recommended_inventory_request.py` is 144 lines. Verified locally with syntax compile, recommended-inventory delegation/initialization tests (3 passed), recommended-inventory behavior/CLI tests (3 passed), full Campaign Factory pytest (575 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 71 — Creator OS recommendation helper extraction | Merged in PR #212 (`57d4795`) | Moves Creator OS winner-expansion recommendations, winner action normalization, best rollup-family selection, creative recommended inventory, and lineage posting-window helpers into `campaign_factory.creator_os_recommendations.CreatorOSRecommendationRepository`, composed by `CoreServices` while keeping private `CampaignFactory` compatibility helpers unchanged. `DailyPlanRepository` now receives the extracted repository methods through the existing callable seams. Net-shrink invariant: `core.py` reduced from 7,877 lines before the slice to 7,745 lines after extraction; `creator_os_recommendations.py` is 188 lines. Verified locally with syntax compile, Creator OS recommendation delegation/initialization tests (3 passed), daily-plan recommendation behavior tests (3 passed), full Campaign Factory pytest (577 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. |
| PR 72 — Account planning and ranking extraction | Merged in PR #213 (`d41e755`) | Moves `account_plan`, `ranking`, ranking score helpers, dashboard rendered-asset enrichment, generated asset lineage synthesis, and account-aware audio recommendation enrichment into `campaign_factory.account_planning.AccountPlanningRepository`, composed by `CoreServices` while keeping public `CampaignFactory` signatures and private compatibility helpers unchanged. The repository receives late-bound facade callables for dashboard/ranking/enrichment seams so existing monkeypatch behavior remains stable. Net-shrink invariant: `core.py` reduced from 7,745 lines before the slice to 7,477 lines after extraction; `account_planning.py` is 366 lines. Verified locally with syntax compile, account-planning delegation/initialization tests (3 passed), account-plan/ranking behavior/API tests (3 passed), full Campaign Factory pytest (579 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch` (rerun serially after parallel fixture race), `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. |
| PR 73 — Publishability support helper extraction | Merged in PR #214 (`6892262`) | Moves latest-audit lookup, active-quarantine lookup, verification/text hash helpers, Instagram post-caption derivation, and caption-lineage sidecar loading into `campaign_factory.publishability.PublishabilityRepository`, composed by `CoreServices` while keeping private `CampaignFactory` compatibility helpers unchanged. Existing publishability methods keep late-bound helper callables to preserve facade monkeypatch seams during this narrow move. Net-shrink invariant: `core.py` reduced from 7,477 lines before the slice to 7,381 lines after extraction; `publishability.py` is 973 lines. Verified locally with syntax compile, publishability delegation tests (2 passed), focused publishability/caption/quarantine behavior tests (22 passed), full Campaign Factory pytest (579 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. |
| PR 74 — Publishability quarantine write extraction | Merged in PR #215 (`99c3554`) | Moves `quarantine_asset` into `campaign_factory.publishability.PublishabilityRepository`, composed by `CoreServices` while keeping the public `CampaignFactory.quarantine_asset` signature unchanged. The repository receives `record_event` through the service seam so quarantine event writes and commit semantics stay centralized and behavior-preserving. Net-shrink invariant: `core.py` reduced from 7,381 lines before the slice to 7,339 lines after extraction; `publishability.py` is 1,042 lines. Verified locally with syntax compile, publishability delegation tests (2 passed), focused publishability/quarantine behavior tests (17 passed), full Campaign Factory pytest (579 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update`, and green PR CI. |
| PR 75 — Export summary facade extraction | Merged in PR #216 (`5daf3d6`) | Adds `campaign_factory.export_summary.ExportSummaryRepository` as a service-level wrapper for existing `campaign_factory.exports` batch summary, daily counters, variant pack grouping, and export manifest behavior. `CampaignFactory` now delegates through `CoreServices` while the repository preserves the old `exports.py` module monkeypatch seam. Net-shrink invariant: `core.py` reduced from 7,339 lines before the slice to 7,331 lines after extraction; `export_summary.py` is 27 lines. Verified locally with syntax compile, export delegation/module-seam tests (3 passed), focused export behavior tests (3 passed), full Campaign Factory pytest (581 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch` (rerun serially after the expected negative-fixture race), `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 76 — Dashboard assembly repository extraction | Merged in PR #217 (`f269eec`) | Moves dashboard payload assembly and default dashboard-campaign selection into `campaign_factory.campaign_overview.CampaignOverviewRepository`, composed by `CoreServices` while keeping public `CampaignFactory.dashboard` and private compatibility helper `_default_dashboard_campaign` unchanged. The repository receives late-bound service callables for list-campaigns, daily counters, creative plan, activity, distribution, trust, and ranking seams so existing facade behavior stays stable. Net-shrink invariant: `core.py` reduced from 7,331 lines before the slice to 7,299 lines after extraction; `campaign_overview.py` is 255 lines. Verified locally with syntax compile, focused dashboard delegation tests (3 passed), focused dashboard behavior tests (3 passed), full Campaign Factory pytest (581 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 77 — Creator OS draft helper repository extraction | Merged in PR #218 (`d12599d`) | Moves Creator OS draft collection, schedule-safe draft filtering, draft exclusion/blocker classification, account surface status normalization, and local schedule-safe asset summaries into `campaign_factory.creator_os_drafts.CreatorOSDraftRepository`, composed by `CoreServices` while keeping private `CampaignFactory` compatibility helpers unchanged. DailyPlan, DraftInventoryGap, and ExecutionReadiness now receive the repository-owned helper methods instead of facade callbacks. Net-shrink invariant: `core.py` reduced from 7,299 lines before the slice to 7,109 lines after extraction; `creator_os_drafts.py` is 240 lines. Verified locally with syntax compile, focused draft-helper delegation tests (9 passed), focused draft inventory/daily plan/execution readiness behavior tests (29 passed), full Campaign Factory pytest (583 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, `pnpm graphify:update` with ignored local graph output, and green PR CI. |
| PR 78 — Creator OS planning-support helper extraction | Merged in PR #219 (`2b01461`) | Moves the remaining Creator OS daily-plan support helpers for target-date normalization, surface summary rollup, recommended story intent/style, draft inventory counts, blocked-account breakdown, manager decision, account state, post time, and recommended post count into `campaign_factory.creator_os_drafts.CreatorOSDraftRepository`, composed by `CoreServices` while keeping private `CampaignFactory` compatibility helpers unchanged. DailyPlan, DecisionLedger, and SurfaceSummary now receive repository-owned helpers instead of facade callbacks. Net-shrink invariant: `core.py` reduced from 7,109 lines before the slice to 6,939 lines after extraction; `creator_os_drafts.py` is 477 lines. Verified locally with syntax compile, focused Creator OS helper delegation tests (8 passed), full extraction facade tests (124 passed), daily-plan/draft-inventory/execution-readiness behavior tests (29 passed), full Campaign Factory pytest (583 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output. |
| PR 79 — Creative learning report facade extraction | Merged in PR #220 (`df00eaa`) | Moves creative knowledge base/report assembly, creative performance analysis, creator learning summary, next content recommendations, learning confidence, fatigue, surface comparison, and recommendation-quality audit public facades into `campaign_factory.creative_knowledge.CreativeKnowledgeRepository`, composed by `CoreServices` while keeping private `CampaignFactory` compatibility helpers and monkeypatch seams unchanged through injected callables. Net-shrink invariant: `core.py` reduced from 6,939 lines before the slice to 6,672 lines after extraction; `creative_knowledge.py` is 788 lines. Verified locally with syntax compile, creative-learning delegation tests (4 passed), existing creative-learning behavior tests (9 passed), full Campaign Factory pytest (585 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output; PR CI green. |
| PR 80 — Creative learning helper-body extraction | Merged in PR #221 (`cf9d9a9`) | Moves creative-learning private helper bodies for knowledge-base building, performance analysis, recommendation explainability, confidence/fatigue scoring, result grouping, and creative result row construction into `campaign_factory.creative_knowledge.CreativeKnowledgeRepository`, composed by `CoreServices` while keeping `CampaignFactory._...` compatibility wrappers and monkeypatch seams stable. Net-shrink invariant: `core.py` reduced from 6,672 lines before the slice to 6,182 lines after extraction; `creative_knowledge.py` is 1,385 lines. Verified locally with syntax compile, creative-learning helper/report delegation tests (6 passed), existing creative-learning behavior tests (9 passed), full Campaign Factory pytest (587 passed), `git diff --check`, `pnpm check:contracts`, `pnpm check:arch`, `pnpm check:arch:fixtures`, `pnpm check:artifacts`, `pnpm check:integration`, `pnpm security:secrets`, `pnpm test`, and `pnpm graphify:update` with ignored local graph output; PR CI green. |
| PR 81 — Final operational helper extraction | In progress on branch `codex/core-final-operational-helper-extraction` | Moves the remaining non-trivial operational helper bodies for strict graph-edge exception recording, Instagram Trial Reel intent validation, and imported-lineage cost recording out of `CampaignFactory` into `CoreServices`, `DistributionRepository`, and `FinishedVideoRepository`. Public/private `CampaignFactory` wrappers remain for compatibility, and `_load_source_lineage` stays facade-owned to preserve the late-bound monkeypatch seam. Net-shrink invariant: `core.py` reduced from 6,182 lines before the slice to 6,103 lines after extraction. Verification in progress: focused facade/service tests and focused trial/cost behavior tests are green. |

---

## Execution mode from PR 44 onward

The extraction pattern is now proven: characterization tests, `CoreServices`, Tier 1, Tier 2, and many Tier 3 repositories are already merged. Future slices should stop optimizing for the smallest possible helper move and instead extract one coherent operational domain per PR. Keep the same behavior-preservation bar, but batch tightly related methods when they share lifecycle, DB tables, tests, and facade boundaries.

This does **not** relax the safety rules. Public `CampaignFactory` signatures stay stable, payloads and DB writes stay stable, monkeypatch compatibility stays intact where tests or callers rely on facade methods, and high fan-in domains still go last. If a grouped slice shows behavior drift or hard cycles, split it smaller and land the safe part.

Preferred next grouping:

1. Extract adjacent reel/finished-video domains in larger coherent PRs:
   - reel input preparation, reel factory execution, and reel output sync
   - parent/variant registration, variant generation, and variant lineage
2. Extract medium-risk operational domains:
   - publishability, rejection evidence, and proof runs
   - recommendation execution, lifecycle, and item payloads
   - archive inventory and candidate quality reporting
3. Extract highest fan-in domains last:
   - inventory
   - lifecycle
   - performance
   - audio
4. End with a final facade cleanup PR that removes dead compatibility shims only when tests prove no public API or behavior drift.

## PR sequence (historical baseline; current work follows the PR 44+ mode above)

1. **PR 0 — characterization harness** (seeded fixture + golden tests for Tier 1+2 entry points). No code moves.
2. **PR 1 — `CoreServices` extraction**: move the 5 shared utilities into an injected service object; `CampaignFactory` holds one `self.services`. Pure plumbing, golden tests unchanged.
3. **PRs 2–5 — Tier 1** (`graph.py`, `events.py`, `models.py`, `asset_import.py`): one module each. `CampaignFactory` delegates. Golden tests green each time.
4. **PRs 6–12 — Tier 2** (creative_planning, reference, caption, distribution, decision_ledger, exceptions, discoverability), breaking the Audio/Performance cycles via injected callables as you go.
5. **PRs 13+ — Tier 3**, one cluster per PR, extracted as facades that compose Tier 1+2. Do **Performance, Lifecycle, Inventory, Audio last** (highest fan-in/helper density). Surface the 6 hidden-region domains explicitly.
6. **Final — `CampaignFactory` is a composition root**: constructs modules, delegates. Target: core.py drops from ~26.7k lines to a thin facade; each module is independently testable. **Campaign Factory maint 4→8, score 6.1→~8+.**

---

## Constraints

- **Public API is frozen.** Every `CampaignFactory.<method>` signature stays; callers (`adapters/`, CLI, stages) must not change. Facade delegates.
- **One coherent domain per PR from PR 44 onward.** Tiny helper-only PRs are still allowed when risk demands it, but the default should be a complete operational slice with characterization + focused behavior tests green before and after; `pnpm check:contracts` green.
- **No behavior change** — this is pure structure. Any behavior fix is a *separate* PR, never bundled into an extraction.
- **No new logic in core.py** during this work (the standing constraint) — new modules only.
- creator-os `main` protected: feature branch → PR; required CI must pass.
- If a cluster won't extract cleanly under its characterization tests, **stop and report** — don't force it. Partial decomposition (Tier 1+2 done, Tier 3 staged) is still a large maint win.

---

## Honest scope

This is **~20+ PRs** and the riskiest track in the repo — but the near-stateless finding makes it mechanical rather than archaeological. It's the only thing that lifts Campaign Factory (the control brain, currently the system's lowest-scoring core component) past ~7.5. Sequence it **after** `TRACK9_RIGOR_PROMPT.md` PR 1 (contract codegen) so the contract layer is drift-proof first, and gate every move behind the characterization net.
